name: Daily Secret Report

on:
  schedule:
    - cron: '0 0 * * *' # Run daily at midnight UTC
  workflow_dispatch:
    inputs:
      scan_count:
        description: 'Number of repositories to scan'
        required: false
        default: '20'

jobs:
  scan-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Discover New Repositories
        run: |
          python scripts/discover_repos.py

      - name: Run Daily Scan
        env:
          SCAN_COUNT: ${{ github.event.inputs.scan_count || '20' }}
        run: |
          python scripts/run_daily_scan.py --scan-count $SCAN_COUNT

      - name: Commit and Push Reports
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add data/target_repos.json data/scan_state.json reports/
          git commit -m "Automated daily secret scan reports and repo updates" || echo "No changes to commit"
          git push

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: "GitHub Secret Report Automation - Run Status: ${{ job.status }}"
          to: ${{ secrets.NOTIFY_EMAIL }}
          from: "GitHub Actions <${{ secrets.MAIL_USERNAME }}>"
          body: |
            The daily secret scan workflow has completed.
            Status: ${{ job.status }}
            Check the repository for the latest reports.
